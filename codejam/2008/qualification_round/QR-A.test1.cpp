#include <cstdio>
#include <vector>
#include <string>

using namespace std;

int main() {
    char p[101];
    vector<string> vs;

    while (true) {
        //if (scanf("%[^\n]", p) != 1) break;
        //getchar();
        if (gets(p) == NULL) break;

       // vs.push_back(p);
    }

    /*for (auto el : vs) {
        printf("string = %s\n", el.c_str());
    }*/

    return 0;
}

